# Pricing and ROI (Archived)

The repository no longer contains the legacy ROI calculator runtime package.

## Current runtime focus
- Deterministic control-loop operations under `/api/v1`
- Telemetry ingestion, optimization decisions, command dispatch, and savings snapshots

## Reference
- Savings endpoint: `GET /api/v1/sites/{site_id}/savings/summary`
- Simulation endpoint: `POST /api/v1/sites/{site_id}/simulation/run`
- Historical notes: `docs/HISTORICAL_APPENDIX.md`
